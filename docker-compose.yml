services:
  kiro-rs:
    build: .
    container_name: kiro-rs
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "8001:8990"
    volumes:
      - ./config/:/app/config/
    restart: unless-stopped
    environment:
      - RUST_LOG=info
